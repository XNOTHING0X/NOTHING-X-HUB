--\
local Players, StarterGui, UserInputService, RunService, Workspace = game:GetService("Players"), game:GetService("StarterGui"), game:GetService("UserInputService"), game:GetService("RunService"), game:GetService("Workspace") local player = Players.LocalPlayer local PlayerGui = player:WaitForChild("PlayerGui") local safeMode = false local hitboxSize = Vector3.new(6, 6, 8) local hitboxTransparency = 1 local holdingWKey, holdingSKey, holdingAKey, holdingDKey = false, false, false, false local Speed = 0.065 local connections = {} local function disconnectAll() for _, connection in pairs(connections) do if connection then connection:Disconnect() end end connections = {} end local function checkDisableControls() return player:GetAttribute("DisableControls") == true end table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed) if gameProcessed then return end if input.KeyCode == Enum.KeyCode.One then safeMode = not safeMode pcall(function() StarterGui:SetCore("SendNotification", { Title = "Safe Mode", Text = safeMode and "ON" or "OFF", Duration = 0.5, Icon = "rbxassetid://140690318962088" }) end) local character = player.Character local hitbox = character and character:FindFirstChild("Hitbox") if hitbox then hitbox.Size = safeMode and hitboxSize or Vector3.new(4.521276473999023, 5.7297587394714355, 2.397878408432007) hitbox.Transparency = safeMode and hitboxTransparency or 1 end elseif input.KeyCode == Enum.KeyCode.L then safeMode = false pcall(function() StarterGui:SetCore("SendNotification", { Title = "Safe Mode", Text = "Destroyed Fully", Duration = 0.5, Icon = "rbxassetid://140690318962088" }) end) local character = player.Character local hitbox = character and character:FindFirstChild("Hitbox") if hitbox then hitbox.Size = Vector3.new(4.521276473999023, 5.7297587394714355, 2.397878408432007) hitbox.Transparency = 1 end disconnectAll() script:Destroy() elseif input.KeyCode == Enum.KeyCode.LeftControl and safeMode and (player.Team == game.Teams.Home or player.Team == game.Teams.Away) then local junk = Workspace:FindFirstChild("Junk") local football = junk and junk:FindFirstChild("Football") local character = player.Character local rootPart = character and character:FindFirstChild("HumanoidRootPart") if football and rootPart then football.CFrame = rootPart.CFrame end elseif input.KeyCode == Enum.KeyCode.P then game:GetService("TeleportService"):Teleport(126195208568849, player) end end)) table.insert(connections, RunService.Stepped:Connect(function() if safeMode and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and not checkDisableControls() then local moveVector = Vector3.zero if holdingWKey then moveVector = moveVector + Vector3.new(0, 0, -Speed) end if holdingSKey then moveVector = moveVector + Vector3.new(0, 0, Speed) end if holdingAKey then moveVector = moveVector + Vector3.new(-Speed, 0, 0) end if holdingDKey then moveVector = moveVector + Vector3.new(Speed, 0, 0) end player.Character.HumanoidRootPart.CFrame *= CFrame.new(moveVector) end end)) table.insert(connections, UserInputService.InputBegan:Connect(function(input, processed) if processed then return end if input.KeyCode == Enum.KeyCode.W then holdingWKey = true elseif input.KeyCode == Enum.KeyCode.S then holdingSKey = true elseif input.KeyCode == Enum.KeyCode.A then holdingAKey = true elseif input.KeyCode == Enum.KeyCode.D then holdingDKey = true end end)) table.insert(connections, UserInputService.InputEnded:Connect(function(input) if input.KeyCode == Enum.KeyCode.W then holdingWKey = false elseif input.KeyCode == Enum.KeyCode.S then holdingSKey = false elseif input.KeyCode == Enum.KeyCode.A then holdingAKey = false elseif input.KeyCode == Enum.KeyCode.D then holdingDKey = false end end)) game:GetService("StarterGui"):SetCore("SendNotification", { Title = "LOAD Safe Mode", Text = "", Duration = 0.5, Icon = "rbxassetid://140690318962088" })
